//enable redirecting users from outside React components, for example
//from the user actions after successful login.


import { createBrowserHistory } from 'history';

export const history = createBrowserHistory();
